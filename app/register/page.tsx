/**
 * @file app/register/page.tsx
 * @description Registration no longer exists as a separate flow.
 * Redirect to /login which handles both new and returning users.
 */

export { default } from '@/app/login/page';
